--
-- source code available for file cap_1_script_2.sql
--
DECLARE
	
	-- Create the variable v_n client that can receive up to 7 integers (9,999,999)
	v_nr_client 	number(7);
	
	-- Creates the variable v_check_stock that can receive 2 possible values: TRUE or FALSE
	v_check_stock	boolean;
	
BEGIN

	-- The NULL; command does nothing, but allows the begin...end; block to execute successfully.
	NULL; -- Within a PL/SQL block it is necessary to have a valid command and "NULL;" is considered one of those commands.
	
END;
